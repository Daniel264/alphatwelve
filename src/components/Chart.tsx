import ChartImage from "../assets/images/chart.svg";

const Chart = () => {
    return (
        <div>
            <h2>Event Registration per month</h2>
            <img src={ChartImage} alt="image of a chart" />
        </div>
    );
};

export default Chart;
